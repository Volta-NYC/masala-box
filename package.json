{
  "name": "masala-box",
  "version": "1.0.0",
  "description": "Masala Box — Contemporary Indian restaurant website",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site, no build needed'",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.1.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18"
  }
}
